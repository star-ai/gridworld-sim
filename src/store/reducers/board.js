const initialState = {
  isRunning: false,
  func: null,
  stateValues: [],
  actionValues: [],
};

const board = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default board;
